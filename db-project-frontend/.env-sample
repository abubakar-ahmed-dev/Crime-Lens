VITE_API_BASE_URL=http://localhost:5001/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Production builds additionally read .env.production (loaded OVER this file),
# which sets VITE_API_BASE_URL=/api so the Dockerized SPA uses its nginx proxy.
