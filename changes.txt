now i want to update the verify agent card information, agent approval popup fields, and all agent records table, and update agent popup fields. Currently the verify agent card and update popup shows password, I want to remove it. Secondly, it shows branch Id. i want branch id and zone name, written with "branch" only, not branch id or branch name. Also, the approval popup shows branch contact to update, but i think it do not update anything. so remove that field. Remove the password field. and update the branch id with branch id and zone name. this field should be drop down with all branches available in db. It should only update the branch id of this police agent, not the zone name or anything else. Also remove the request date field. so it should only show branch and username fields in the approval popup with correct functionality.

Also in the all agent table i want to remove the password field, and add zone name field, it should show the name of zone that the agent is registered in. The table should show agent id, username, zone, branch id, branch contact and date of creation.

The password field should be removed from The updation popup as well, and it should only show the username and branch field, same as approval popup. You can use same popup as you done with crime approval and updation popup, if possible. 

For all these, you can check current implementation and analyze how it can be done , and safely removing extra code if needed